{
  "name": "static-site-template",
  "version": "1.0.0",
  "description": "see using github template repos to jump-start static site projects",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^0.12.1"
  }
}
